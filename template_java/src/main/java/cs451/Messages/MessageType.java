package cs451.Messages;

/**
 * Represents the two types of messages
 * ACK : an acknowledgment of a BROADCAST message
 * BROADCAST : a message containing data
 */
public enum MessageType {
    ACK, BROADCAST
}